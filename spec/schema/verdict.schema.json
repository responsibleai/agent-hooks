{
  "$id": "https://responsibleai.github.io/agent-hooks/schema/v0.1/verdict.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Agent Hooks — verdict",
  "description": "Interceptor return value (AGENT-HOOKS-0.1 §5). Three decisions: allow (absorbs warn via warnings[]), deny (absorbs escalate via the approval block), transform. A verdict carries decision content only; composition is host configuration (§7.1).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "decision"
  ],
  "properties": {
    "decision": {
      "type": "string",
      "enum": [
        "allow",
        "deny",
        "transform"
      ]
    },
    "reason": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^(?!host_error:).*",
      "description": "Interceptor-supplied machine identifier. The host_error: prefix is reserved for host-synthesized failures (§11)."
    },
    "message": {
      "type": [
        "string",
        "null"
      ],
      "description": "Human-readable text for the caller."
    },
    "warnings": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "pattern": "^(?!host_error:).*"
          },
          "message": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "description": "Recorded concerns that do not affect control flow (§5.1). Permitted on any decision; unioned across an emission's verdicts (§7.3)."
    },
    "approval": {
      "type": "object",
      "description": "Present only on deny: marks the deny as liftable by the approval seam (§9). MAY be empty; reserved for approver-facing parameters."
    },
    "transform": {
      "$ref": "#/$defs/transform"
    },
    "evidence": {
      "$ref": "#/$defs/evidence"
    },
    "result_labels": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      },
      "description": "Interceptor-supplied labels the host MUST persist with the produced data and resurface on later interception points (§5.4)."
    }
  },
  "allOf": [
    {
      "description": "transform body REQUIRED iff decision == transform (§5).",
      "if": {
        "properties": {
          "decision": {
            "const": "transform"
          }
        }
      },
      "then": {
        "required": [
          "transform"
        ]
      },
      "else": {
        "not": {
          "required": [
            "transform"
          ]
        }
      }
    },
    {
      "description": "approval block permitted only on deny (§5.1).",
      "if": {
        "required": [
          "approval"
        ]
      },
      "then": {
        "properties": {
          "decision": {
            "const": "deny"
          }
        }
      }
    }
  ],
  "$defs": {
    "transform": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path"
      ],
      "properties": {
        "path": {
          "type": "string",
          "pattern": "^\\$(target|policy_target)(\\.|\\[|$)",
          "description": "JSONPath rooted at $target (§5.2). $policy_target is a deprecated alias accepted through agent-hooks v0.2."
        },
        "value": {
          "description": "Any JSON value, including null. An absent member is equivalent to null (§5.2)."
        }
      }
    },
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "artefact": {
          "type": [
            "string",
            "null"
          ],
          "description": "Content address (sha256:<hex>) or URI of an offline-verifiable proof."
        },
        "verification_pointers": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Named URIs an auditor MAY consult. Hosts MUST NOT dereference automatically (§14)."
        }
      }
    }
  }
}
